<Window x:Class="BindingDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BindingDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <local:MojeBarvy x:Key="Barvicky"  BarvaPozadi="LightGreen" />
        <local:CisloToBarvaConvertor x:Key="CisloToBarva" />
    </Window.Resources>
    <Grid x:Name="MujGrid" DataContext="{StaticResource Barvicky}">
        <StackPanel Width="200" >
            <TextBox Foreground="{Binding BarvaPisma}"
                     Background="{Binding Cislo, Convertor={StaticResource CisloToBarva}}" />
            <Button Click="Button_Click" Content="Akce" />
        </StackPanel>
    </Grid>
</Window>
